version: 2
updates:
  # The app has no Python dependencies of its own; what needs watching is the
  # actions this repository runs, which are code from other people.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
    labels: ["dependencies"]
